/* writelines: write the lines onto the screen 
 *
 * lines:    the array of pointer points to the beginning of each line 
 * numlines: the number of lines
 * */
void writelines(char *lines[], int numlines);
